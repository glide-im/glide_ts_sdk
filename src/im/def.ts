
// use for cache
export interface GlideUserInfo {
    uid: string;
    name: string;
    avatar: string;
}

// use for cache
export interface GlideChannelInfo {
    id: string;
    name: string;
    avatar: string;
}